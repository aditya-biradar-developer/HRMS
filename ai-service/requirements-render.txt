# AI Service Dependencies for Render
flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Document Processing
PyPDF2==3.0.1
python-docx==1.1.0

# Utilities
requests==2.31.0

# AI Models
google-generativeai>=0.3.0
huggingface-hub>=0.20.0

# Note: Removed heavy dependencies (deepface, opencv, tensorflow)
# These can be added back if needed, but they significantly increase build time
# and may cause memory issues on free tier
